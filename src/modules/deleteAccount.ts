import { deleteAccount, get } from "./fetch.ts";

const deleteAccountBtn = document.querySelector('.deleteAccountButton') as HTMLButtonElement;

deleteAccountBtn.addEventListener('submit', () => {

})